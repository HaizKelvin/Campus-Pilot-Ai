{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Stores student data and subscription status",
      "type": "object",
      "properties": {
        "profile": { 
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "university": { "type": "string" },
            "year": { "type": "number" },
            "isInternational": { "type": "boolean" },
            "country": { "type": "string" },
            "profileImageUrl": { "type": "string" }
          }
        },
        "academics": { "type": "object" },
        "finance": { "type": "object" },
        "health": { "type": "object" },
        "wellbeing": { "type": "object" },
        "subscription": {
          "type": "object",
          "properties": {
            "status": { "type": "string", "enum": ["trial", "active", "expired"] },
            "startDate": { "type": "string", "format": "date-time" }
          }
        },
        "webauthnCredentials": {
          "type": "array",
          "items": { "type": "object" }
        }
      },
      "required": ["profile"]
    },
    "Chat": {
      "title": "Chat Message",
      "description": "Conversation history with CampusPilot AI",
      "type": "object",
      "properties": {
        "role": { "type": "string", "enum": ["user", "model"] },
        "content": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      }
    },
    "SystemHealth": {
      "title": "System Health",
      "description": "Used for connectivity verification",
      "type": "object",
      "properties": {
        "status": { "type": "string" }
      }
    }
  },
  "firestore": {
    "/system/health": {
      "schema": "SystemHealth",
      "description": "A static document to check database connectivity"
    },
    "/users/{userId}": {
      "schema": "User",
      "description": "All student telemetry data indexed by UID"
    },
    "/users/{userId}/chats/{chatId}": {
      "schema": "Chat",
      "description": "Individual chat messages for the student"
    }
  }
}
